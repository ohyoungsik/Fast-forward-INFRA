[Unit]
Description=FastAPI App
After=network.target

[Service]
User=ansible_3tier
WorkingDirectory=/home/ansible_3tier/app
ExecStart=/usr/bin/uvicorn main:app --host 0.0.0.0 --port 8000

[Install]
WantedBy=multi-user.target